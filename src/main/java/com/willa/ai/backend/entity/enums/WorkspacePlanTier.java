package com.willa.ai.backend.entity.enums;

/** Gói workspace (tách biệt gói feedback/chat: Free, Student, Pro). */
public enum WorkspacePlanTier {
    FREE_WORKSPACE,
    STUDENT_WORKSPACE,
    PRO_WORKSPACE
}
