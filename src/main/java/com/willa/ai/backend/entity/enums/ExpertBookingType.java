package com.willa.ai.backend.entity.enums;

public enum ExpertBookingType {
    /** Review ấn phẩm — giá cố định theo expert. */
    REVIEW,
    /** Trao đổi thêm theo giờ — giá × số giờ (tùy expert). */
    HOURLY
}
